import { ConvocatoriasScreen } from '@/screens/family/convocatorias';

/** O2-5 E1 — convocatorias del hijo activo (próximas + histórico). */
export default function Screen() {
  return <ConvocatoriasScreen />;
}
