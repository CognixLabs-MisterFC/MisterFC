import { AsistenciaScreen } from '@/screens/family/asistencia';

/** O2-5 E1 — histórico de asistencia del hijo activo (consulta). */
export default function Screen() {
  return <AsistenciaScreen />;
}
