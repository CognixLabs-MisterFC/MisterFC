import { DireccionJugadorFichaScreen } from '@/screens/direction/jugador-ficha';

/** O2-11a-2 — Ficha de jugador (oculta, ?playerId), solo lectura club-wide. */
export default function Screen() {
  return <DireccionJugadorFichaScreen />;
}
