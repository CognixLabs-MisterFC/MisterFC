import { DireccionDashboardScreen } from '@/screens/direction/dashboard';

/** O2-11b — Dashboard ejecutivo (solo lectura, club-wide). */
export default function Screen() {
  return <DireccionDashboardScreen />;
}
