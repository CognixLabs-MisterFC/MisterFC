import { DireccionInvitationsScreen } from '@/screens/direction/invitations-list';

/** D2-2 — Invitaciones pendientes del club (SOLO LECTURA, club-wide, sin detalle). */
export default function Screen() {
  return <DireccionInvitationsScreen />;
}
