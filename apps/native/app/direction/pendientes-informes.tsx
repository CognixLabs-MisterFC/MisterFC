import { DireccionReportsProgressScreen } from '@/screens/direction/reports-progress-list';

/** D2-2 — Progreso de informes por equipo y campaña (SOLO LECTURA, sin detalle). */
export default function Screen() {
  return <DireccionReportsProgressScreen />;
}
