import { MensajeNuevoScreen } from '@/screens/staff/mensaje-nuevo';

/** O2-11a-1 — "Nueva conversación" de dirección (reuso staff, basePath /direction). */
export default function Screen() {
  return <MensajeNuevoScreen basePath="/direction" />;
}
