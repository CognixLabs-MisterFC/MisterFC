import { MensajeNuevoScreen } from '@/screens/staff/mensaje-nuevo';

/** O2-10b-1a — "Nueva conversación" del staff (1:1 con jugador o chat de equipo). */
export default function Screen() {
  return <MensajeNuevoScreen />;
}
