import { StaffMensajesScreen } from '@/screens/staff/mensajes';

/** O2-10b-1a — inbox del staff (1:1 + equipo) con "Nueva conversación". */
export default function Screen() {
  return <StaffMensajesScreen />;
}
