import { StaffInformesScreen } from '@/screens/staff/informes';

/**
 * 19-C — Entrada de menú "Informes" del cuerpo técnico: estado de los informes de la
 * campaña por SUS equipos (dispatcher: sin campaña / 0 equipos / 1 directo / varios picker).
 */
export default function Screen() {
  return <StaffInformesScreen />;
}
