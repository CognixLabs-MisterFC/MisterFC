import { CuerpoTecnicoDireccionScreen } from '@/screens/staff/cuerpo-tecnico-direccion';

/** O2-10b-2 — cuerpo técnico de dirección (coordinador, consulta del equipo activo). */
export default function Screen() {
  return <CuerpoTecnicoDireccionScreen />;
}
